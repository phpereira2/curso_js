let funcaoPotencia = function potencia(base, potencia=0) {
    return Math.pow(base, potencia); // Uma outra maneira é: return (base**potencia);
}

console.log(funcaoPotencia(3, 2));
console.log(typeof(funcaoPotencia));